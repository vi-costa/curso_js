function soma(x, y) {
  return x + y;
}
let n1 = parseInt(Math.random() * (100 - 1) + 1);
let n2 = parseInt(Math.random() * (100 - 1) + 1);

console.log(`${n1} + ${n2} =`, soma(n1, n2));
//soma de dois numeros aleatorios
