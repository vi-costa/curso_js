const frutas = ['Pera', 'Maçã', 'Uva'];

for (let i of frutas) { 
    console.log(i); //of retorna o value
                    // de um vetor ou variavel
}

frutas.forEach(function(elemento){
    console.log(elemento);

});